Profolyo — 15 portfolio templates as standalone HTML.
Each file is self-contained: all styles inline, no JS, no external files.
Fonts load from Google Fonts (exact typography) with system fallbacks offline.
